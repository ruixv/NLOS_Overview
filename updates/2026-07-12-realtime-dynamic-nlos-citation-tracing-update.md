# 12 July 2026 real-time dynamic NLOS citation-tracing update

## Citation-tracing result

A forward/reference-tracing pass through recent ToF NLOS comparative literature surfaced **Low-latency time-of-flight non-line-of-sight imaging at 5 frames per second** by Nam et al. The paper was already cited in legacy survey tables but was missing from the README and homepage and did not have an explicit literature-review sentence describing its system-level contribution.

The final publication is **Low-latency time-of-flight non-line-of-sight imaging at 5 frames per second**, Nature Communications 12, 6526 (2021), DOI `10.1038/s41467-021-26721-x`. The repository therefore uses the publisher title, full eight-author list, and journal venue rather than the shorter 2020 arXiv metadata.

## Why it belongs

The work combines two gated $16\times1$ SPAD arrays (28 active pixels), sparse relay-wall scanning, virtual-aperture remapping, and accelerated phasor-field propagation. It reports end-to-end acquisition and reconstruction of diffuse, non-retroreflective dynamic hidden scenes at 5 fps with approximately 1 s latency, making it a milestone between fast reconstruction algorithms and later room-scale NLOS video systems.

## Synchronized artifacts

- `README.md`: final publisher title, final journal venue, contribution summary, and a 2021 milestone-timeline node.
- `index.html`: searchable/latest paper object, updated 2021 trajectory, and latest-entry count 85 to 86.
- `article/2active.tex`: contribution inserted in the SPAD-array / dynamic-video discussion under the canonical key `nam_real-time_2020`.
- `egbib_20260712_realtime_updates.bib`: final title, full eight-author list, Nature Communications volume/article number, DOI, and publisher URL.
- `egbib_merged_20260711.bib`: regenerated by the duplicate-free bibliography merger; the canonical key is reused so the paper appears only once in the compiled references.

## PDF status

A clean 31-page survey PDF had already been rebuilt and validated after integrating the feature-visibility and real-time-system literature-review paragraphs. After that build, a final publisher-metadata audit corrected the Nam et al. reference from its shorter preprint title and six-author metadata to the Nature Communications title and complete eight-author list. The corrected text sources and bibliography are committed, but no later binary commit containing a newly regenerated `bare_jrnl.pdf` has yet been verified on the default branch. Therefore the repository must not claim that the current binary necessarily contains the final corrected title and full author list.

The committed workflow `.github/workflows/nlos-realtime-dynamic-sync.yml` performs the remaining clean LaTeX/BibTeX rebuild and checks the canonical key and final title in `bare_jrnl.aux`/`bare_jrnl.bbl` before committing the replacement PDF. Until that binary commit appears, README, homepage, survey source, and bibliography are synchronized, while the PDF has an explicit pending-rebuild exception recorded here.
