Trigger the same-repository pull-request workflow that applies the marker-based pending NLOS updates, rebuilds the LaTeX survey, validates the generated PDF, publishes the synchronized artifacts to master, and removes this temporary trigger.
