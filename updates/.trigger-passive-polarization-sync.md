Retrigger the same-repository pull-request workflow after adding explicit LaTeX diagnostics. The workflow applies the marker-based pending NLOS updates, rebuilds and validates the survey PDF, publishes synchronized artifacts to master on success, and removes this temporary trigger.
