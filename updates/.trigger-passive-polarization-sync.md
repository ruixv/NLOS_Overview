Retrigger the same-repository pull-request workflow after replacing line-wrap-sensitive PDF text validation with robust citation-key and rendered-output checks. The workflow applies all pending marker-based NLOS updates, rebuilds and validates the survey PDF, publishes synchronized artifacts to master on success, and removes this temporary trigger.
