Retrigger the validated NLOS synchronization after adding a rebase-before-push step. The workflow applies all pending marker-based updates, clean-builds and validates the survey PDF, publishes synchronized artifacts to master, and removes this temporary trigger.
